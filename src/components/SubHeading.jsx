import React from 'react'

const SubHeading = ({className,text}) => {
  return (
    <>
    <h2 className={`text-dm text-39 font-bold ${className}`}>{text}</h2>
    </>
  )
}

export default SubHeading